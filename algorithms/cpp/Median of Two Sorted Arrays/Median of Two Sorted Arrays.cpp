// Median of Two Sorted Arrays.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"

class Solution {
public:
    double findMedianSortedArrays(int A[], int m, int B[], int n) {
        // Start typing your C/C++ solution below
        // DO NOT write int main() function
        int medianA, medianB;
		int leftA, rightA, leftB, rightB;
		
		medianA = m / 2;
		medianB = n / 2;
		if (medianA < medianB)	
		{
			// to do
		}
    }
};

int _tmain(int argc, _TCHAR* argv[])
{
	return 0;
}

